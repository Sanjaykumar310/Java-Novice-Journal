what is meant by methods in java ?

    * The methods means it is named block of code used to perform some specific task ,
    * It can accepts input through the parameterized and execute statements,return values also.

Types of methods ,

    * Parameterized method,
    * No Arguments.

* Parameterized Methods ,

    * The parameterized Methods accepts one or more parameterized,
    * The values passed while calling the method are called arguments.

    syntax

        returnType methodName(dataType parameter1, dataType parameter2) {
    // method body
    }

* no arguments,

    * The no argument methods it does not take any input (parameterized).when called it perform some spectfic task at every time unless it object fields.

    syntax 
        returnType methodName() {
    // method body
    }