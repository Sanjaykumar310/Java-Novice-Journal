Polymorphism 

what is meant by polymorphism ?
    * poly means many , morphism means is forms polymorphism is manyforms.
    * polymorphism means same method perform diffrent actions depending on objects.

Types of polymorphism,

    * compile time polymorphism (method overloading),
    * Runtime polymorphism (method overriding).

Method overloading (complie time polymorphism),
    
     The method overloding means the method name should be same and different paramerized with in same class is known as the method overloading or complie time polymorphism.

Method overriding (Runtime polymorphism),

    The method overriding means where the child class provides its own implemntations of method that already defined in paraent class (same method ,different implementation ) is known as the method overriding.